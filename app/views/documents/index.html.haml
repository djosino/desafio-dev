%h3 Meus Documentos

%br

%table
  %thead
    %tr
      %th Usuário
      %th Status
      %th Arquivo
      %th Data de Criação
      %th{colspan: 99}
        Ações
  %tbody
    - @documents.each do |document|
      %tr
        %td= document.user.try(:email)
        %td= document.status
        %td= link_to 'Download', document.file
        %td=l document.created_at, format: :short
        %td= link_to 'Visualizar', document

%br

= link_to 'Novo Documento', new_document_path
